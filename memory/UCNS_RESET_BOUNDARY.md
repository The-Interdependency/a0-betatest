# A0 UCNS reset boundary

Date: 2026-07-21

## Current state

A0 preserves the exact public 157-glyph gonol as source provenance from
`The-Interdependency/a0-betatest@7af8debf6ef3905f01baff02b43d8c3bee16ccbc`.

That fixture is not itself a current UCNS object.

The former external `ucns==0.8.3` package and the later pre-reset repository
lineage omit the intrinsic Möbius twist/seam required by current UCNS canon.
They are archive evidence and are not installed or activated by A0.

## Active A0 surfaces

- `interdependent_lib.gonal.gonal.PUBLIC_GONOL_157` — immutable source fixture.
- `PrivateGonal` — A0-local fixed-origin phase/permutation application state.
- `phase_embedding` — hash-derived A0 phase lanes.
- `interaction_heuristics` — A0-local text features, not EDCM measurement.
- `A0StructuralShape` — content identity for PCTA/PTCA layers with
  `ucns_state = "NA"`.

## Unavailable surfaces

A0 does not currently expose:

- a UCNS object constructor;
- UCNS multiplication or quotient;
- a UCNS unit;
- factorization or primality;
- theorem or proof status;
- a continuous public-gonol rotation bridge;
- a double-cover claim derived from local array shape.

## Reactivation rule

UCNS integration may return only through a versioned producer contract that:

1. identifies the new twist-bearing object epoch;
2. makes the hidden seam intrinsic and immovable;
3. represents orientation and 720-degree return;
4. defines complete recursive payloads;
5. publishes versioned serialization and migration schemas;
6. carries explicit theorem-status and measurement firewalls; and
7. supplies deterministic conformance fixtures.

## hmmm

The exact source fixture is preserved. The lawful projection from that fixture
into the restarted UCNS object remains unresolved.
